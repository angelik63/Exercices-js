let number = [8, 6, 4, 9];
let tab = number.map((item) => item * 2);

console.log(tab);

let reservation = {
    nomClient :"Connor",
    numeroTable : 5,
    nombrePersonnes : 4
};
const Myname ="Maxime";
console.log(`Bonjour ${Myname}, la réservation pour ${reservation.nomClient} (${reservation.nombrePersonnes} personnes) est confirmée à la table <${reservation.nombrePersonnes}>.`);


